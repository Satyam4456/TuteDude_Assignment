## This program include two tasks:

### First Task: Takes a number from user

    - Then using If-Else Condition checks whether the numbwe is an ODD number or an Even number.
    - Then Display the result as: "Number is an Odd number."

### Second task:

    - It don't take any user input, it just calculates the total sum of number from 1 to 50 using while loop.